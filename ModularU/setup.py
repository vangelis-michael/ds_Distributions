from setuptools import setup

setup(name='ammi_distributions', 
      version='1.0.1', 
      description='Gaussian and Binomial Distributions', 
      packages=['ammi_distributions'], 
      author = 'Vangelis Oden',
      author_email = 'omichael@aimsammi.org',
      zip_safe=False) # Zip allows the file not to be run from a zip file